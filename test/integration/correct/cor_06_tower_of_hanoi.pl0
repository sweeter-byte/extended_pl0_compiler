program hanoi;
procedure move(n, src, dst, aux);
begin
  if n > 0 then begin
    call move(n - 1, src, aux, dst);
    write(n); { Move disk n... just print n for trace }
    call move(n - 1, aux, dst, src);
  end;
end;
begin
  call move(3, 1, 3, 2);
end